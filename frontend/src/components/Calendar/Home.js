import React, { useEffect,useState } from 'react';const  {
    REACT_APP_BACKEND
} = process.env;


function Login({redirect=true}) {
     return (
    <div style={{ textAlign: 'center', marginTop: '5%' }}>
      <h2 style={{ marginBottom: '10px' }}>Calendario</h2>

    </div>
  );
}

export default Login;
